import {  createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      background: {
          default: "#263238"
        },
        text: {
          primary: "#ededed"
        },
    },
  });
  
export default theme;